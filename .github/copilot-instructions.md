# Smart Medical Diagnosis

- Backend: FastAPI with a scikit-learn symptom classifier.
- The model uses deterministic synthetic training data for demonstration.
- Keep medical disclaimers visible: predictions are educational screening, not medical advice.
- Run the API from the repository root with `uvicorn backend.app.main:app --reload`.
